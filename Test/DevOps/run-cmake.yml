parameters:
  CMAKE_PATH: 'cmake'
  CTEST_PATH: 'ctest'
  DREAM3D_SDK: ''

steps:
- script: |
    echo CMake Path: $(CMAKE_PATH)
    $(CMAKE_PATH) -G Ninja -DDREAM3D_SDK=$(DREAM3D_SDK) -DCMAKE_BUILD_TYPE=Release $(Build.Repository.LocalPath)
  displayName: 'CMake: configure'
# - script: |
#     cd $(Build.BinariesDirectory)
#     $(CMAKE_PATH) --build .
#   displayName: 'Build DREAM3D....'
- script: |
    cd $(Build.BinariesDirectory)
    $(CTEST_PATH) -D Experimental
  displayName: 'CMake: ctest'